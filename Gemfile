source "https://rubygems.org"

# Use the same Ruby version as the project/vendor bundle if known
ruby '3.1.2'

# Jekyll and supporting plugins/themes
gem "jekyll", "~> 3.10"
gem "jekyll-remote-theme"
gem "jekyll-feed"
gem "jekyll-sitemap"
gem "kramdown-parser-gfm"

# Include Minimal Mistakes as a gem so the theme's assets are available locally.
# You can remove this if you prefer to use `remote_theme` only.
gem "minimal-mistakes-jekyll", "~> 4.24.0"

# If you prefer GitHub Pages environment, replace the above with:
# gem "github-pages", group: :jekyll_plugins
